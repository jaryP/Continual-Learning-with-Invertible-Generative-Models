cl_technique_config:
  name: naive
train_config:
  epochs: 5
  load: Yes
  save_path: experiments/results/mnist/mt/no_shuffle/naive
  experiments: 5
  cuda: Yes
  batch_size: 64
  save: Yes
  optimizer: adam
  lr: 0.001
cl_config:
  label_per_task: 2
  cl_problem: MT
  shuffle_labels: No
  regularize: Yes
  dataset: MNIST
