cl_technique_config:
  name: prer
  z_dim: 10
  blocks: 10
  n_levels: 3
  autoencoder_epochs: 2
  generator_epochs: 20
  rec_loss: mse
  conditioning_config:
    type: one_hot
    dimension: 10
  weights:
    er: 1
    l1: 0.001
train_config:
  epochs: 5
  load: No
  save_path: experiments/results/opt/mnist
  experiments: 5
  cuda: Yes
  batch_size: 128
  save: No
  optimizer: adam
  lr: 0.001
cl_config:
  label_per_task: 2
  cl_problem: MT
  shuffle_labels: No
  regularize: Yes
  dataset: mnist
