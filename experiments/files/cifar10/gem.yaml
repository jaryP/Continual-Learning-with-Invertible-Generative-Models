cl_technique_config:
  name: gem
  task_memory_size: 500
  sample_size: 50
train_config:
  epochs: 20
  load: Yes
  save_path: experiments/results/cifar10/gem
  experiments: 5
  cuda: Yes
  batch_size: 64
  save: Yes
  optimizer: adam
  lr: 0.001
cl_config:
  label_per_task: 2
  cl_problem: MT
  shuffle_labels: No
  regularize: Yes
  dataset: cifar10
